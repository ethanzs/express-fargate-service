{
  "name": "express-fargate-services",
  "version": "1.0.0",
  "description": "npm workspace: shared internal package + the api and hydrator services",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "shared",
    "api",
    "hydrator"
  ],
  "scripts": {
    "build": "npm run build -w shared && npm run build -w api -w hydrator",
    "typecheck": "npm run build -w shared && npm run typecheck -w api -w hydrator",
    "test": "npm run build -w shared && npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces",
    "format:check": "npm run format:check --workspaces",
    "prepare": "cd .. && husky code/.husky || true"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@semantic-release/exec": "^7.1.0",
    "husky": "^9.1.7",
    "semantic-release": "^25.0.7"
  },
  "overrides": {
    "@esbuild-kit/core-utils": {
      "esbuild": "^0.25.0"
    }
  }
}
